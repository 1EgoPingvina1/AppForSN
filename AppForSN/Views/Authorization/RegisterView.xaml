<Window x:Class="AppForSNForUsers.Views.Authorization.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppForSNForUsers.Views.Authorization"
        mc:Ignorable="d"
        Title="RegisterView" Height="450" Width="800">
    
    <Grid Margin="20,20,10,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Регистрация" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Stretch">
            <TextBox Style="{StaticResource SteamTextBoxStyle}"  Text="{Binding FirstName}" Margin="0,5"/>
            <TextBox Style="{StaticResource SteamTextBoxStyle}"  Text="{Binding SecondName}" Margin="0,5"/>
            <TextBox Style="{StaticResource SteamTextBoxStyle}"  Text="{Binding LastName}" Margin="0,5"/>
            <ComboBox SelectedItem="{Binding Gender}" Margin="0,5">
                <ComboBoxItem Content="Мужской"/>
                <ComboBoxItem Content="Женский"/>
                <ComboBoxItem Content="Другой"/>
            </ComboBox>
            
            <CheckBox Content="Я автор проектов" IsChecked="{Binding IsAuthor}" Margin="0,5"/>
            <TextBox Style="{StaticResource SteamTextBoxStyle}"  Text="{Binding Username}" Margin="0,5"/>
            <PasswordBox Style="{StaticResource SteamPasswordBoxStyle}" x:Name="PasswordBox" Margin="0,5"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Зарегистрироваться" Margin="0,20,0,0" Height="35" Style="{StaticResource SteamPrimaryButton}"/>
    </Grid>
</Window>
