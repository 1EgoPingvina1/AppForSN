<Window x:Class="AppForSNForUsers.Views.Dashboard.MainDashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:AppForSNForUsers.Views.Dashboard"
        xmlns:shared="clr-namespace:AppForSNForUsers.Views.Shared"
        mc:Ignorable="d"
        Title="MainDashboardView"
        MinWidth="600" MinHeight="400"
        d:DesignHeight="450" d:DesignWidth="800"
        WindowStyle="None">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Боковое меню -->
        <StackPanel Grid.Column="0" Background="#2A475E" Width="200">
            <Button Content="Главная" Command="{Binding NavigateCommand}" CommandParameter="Home"/>
            <Button Content="Профиль" Command="{Binding NavigateCommand}" CommandParameter="Profile"/>
            <Button Content="Настройки" Command="{Binding NavigateCommand}" CommandParameter="Settings"/>
        </StackPanel>

        <!-- Контейнер для страниц -->
        <Frame Grid.Column="1" 
               NavigationUIVisibility="Hidden"
               Content="{Binding CurrentPage}"/>
    </Grid>
</Window>
